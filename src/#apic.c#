#include "apic.h"
#include "acpi.h"
#include "x86.h"

void disable_pic(void) {
    outb(PIC1_DATA, 0xFF);
    outb(PIC2_DATA, 0xFF);
}

void init_apic(void) {
    struct MADT *madt = acpi_tables.madt;
    if (madt == 0)
        return;
    
}    
